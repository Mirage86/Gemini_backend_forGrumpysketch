import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bauer.grumpysketch.app',
  appName: 'Grumpy Sketch',
  webDir: 'dist'
};

export default config;
